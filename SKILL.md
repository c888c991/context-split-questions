---
name: context-split-questions
description: 当一次任务需要在大量材料（长文档、会议记录、聊天历史、大代码库）里检索证据，或问题含 3 个及以上独立取证方向（每个方向要分别检索取证、同一份证据无法同时回答）时使用：拆子问题、按语义切材料并做保真摘要、只把相关证据读入上下文、汇总时校验冲突与时效。材料能一次读完且各方向共用同一份证据、子问题无法独立回答、或只是要换会话时不要使用。
---

# 拆分问题与上下文防腐

上下文腐烂不是"字太多"，而是四件事同时发生：无关材料混进来、被推翻的结论还留着、同一事实出现互相矛盾的版本、中间产物没落盘导致无法复核和交接。

所以本 Skill 的做法是：只把与当前子问题相关的证据读进上下文，让每一步结论都能核对，并把中间产物写到磁盘上，使任务随时可以交接。

## 何时启用

- 问题包含 ≥3 个独立取证方向，且每个方向需要分别检索/取证，而不是同一份证据能同时回答。
- 材料读不完：长文档、会议记录、聊天历史、大代码库，一两次读不完。
- 两者同时出现。

上面两条与开头的 `description` 必须保持一致，改动时两处一起改。

## 何时不要启用

- 材料能一次读完，且各方向共用同一份证据、无需分别检索。
- 子问题高度耦合，必须同时看完全部材料才能回答。
- 只是想换个会话继续聊，不是在回答新问题。

## 硬规则（优先于下面的流程）

1. 只把与当前子问题相关的证据读进上下文；无关材料不读、不贴。
2. 每条结论附证据编号与来源；证据不足就写「材料不足」，不要补编。
3. 证据互相矛盾、或时效不同时并列列出并标注日期，不要擅自挑一个。
4. 中间产物落盘，对话里只留索引（块 ID + 主题 + 一行结论），不留全文。
5. 不代替用户新建会话、切换会话或执行压缩；只输出提醒。
6. 先给出子问题清单，等用户确认后再动手。例外只有两种：用户明确说"直接做完"，或运行在自动化任务里（没有用户可回应）；例外情况先把清单落盘再继续，并在产出里说明跳过了确认。
7. 遵循用户既有的上下文与会话规则（例如项目的 AGENTS.md），不要另立一套阈值。

## 落盘约定

工作目录 `work/answers/<任务名>/`，任务名用 ASCII 与连字符（例如 `proj-a-delay`）：

```text
source/             原始材料落盘（材料不在磁盘上时才需要）
00-子问题清单.md    子问题、依赖关系、状态
chunks/<块ID>.md    每块材料的保真摘要
answers/Q<n>.md     每个子问题的答案
汇总报告.md         最终交付
```

- 用户或项目已有约定目录（如 `docs/`、`docs/progress/`）时优先用它们，不另立体系。
- 用默认目录、且当前在 git 仓库里时，提醒用户把 `work/answers/` 加进 `.gitignore`，别让中间产物混进提交。
- 摘要生成时直接写文件，回到对话的只有索引行。
- 新会话接手只需读 `00-子问题清单.md` 和相关 `chunks/`，不必重读原文。
- 阶段级交接写用户既有的进度报告位置（如 `docs/progress/`）。
- 不要写到仓库外。

## 流程

### 1. 拆子问题

子问题必须可独立回答、可验证、有明确输出。每个子问题写清三件事：

- 需要什么信息；
- 输出格式：列表 / 表格 / 时间线 / 结论；
- 依赖哪个子问题（例如 Q3 依赖 Q1 和 Q2）。

编号 Q1..Qn，标出依赖关系，写进 `00-子问题清单.md` 后交给用户确认。完整示例见 `references/decomposition-example.md`。

拆的过程中如果发现材料其实能一次读完、或子问题没法独立回答，就停止拆分：说明原因，退回普通做法，不要为了走流程硬拆。

### 2. 材料落盘、按语义切块并做保真摘要

材料不在磁盘上时（用户粘在对话里的文本、MCP 资源、截图、扫描件），先原样落盘到 `source/`，再切块；否则后面的 `rg` 和 `路径:行号` 都无从谈起。图片和扫描件先转成文字（pdf / documents 技能，或请用户提供文本版），不要把图片本身读进上下文。

按语义边界切，不要按字数硬切：章节与标题、会议日期与发言人、文档类型（合同 / 邮件 / 日志 / 报告）、时间线阶段、主题（付款 / 交付 / 审批 / 变更）。

一块 = 一个能独立引用的话题单元，通常一屏以内；宁可整段，也不要切半句。

每块写进 `chunks/<块ID>.md`，带唯一 ID（如 `doc3-p12`）、来源、日期、主题、涉及子问题、是否已被后续材料替代。模板与好坏示例见 `references/faithful-summary.md`。

核心要求：数字、日期、人名、否定词、条件、因果、决策必须保留，不能只写"讲了项目延期"。

全部切块完成后，再统一扫一遍 `chunks/`，回填每块的【是否已被替代】：单块刚生成时填不准，只有看到后续材料，才知道哪块被哪块替代。汇总时以此判断新旧。

### 3. 按子问题取证据

- 先定位文件：`rg -l` 只看命中的文件；需要行号时用 `rg -n`。
- 再按命中位置小范围精读（文件 + 行范围），不要整篇读入。
- 够用即止，通常 3–5 块；时间线、追溯类子问题可能更多，但每块只引用必要片段，不整块搬，也不要"以防万一"多读。
- 每条证据编号写成 `Q<n>-[1]`、`Q<n>-[2]`…（前缀是当前子问题号，避免不同子问题的编号相撞），并带来源（`路径:行号`，或 文档名 + 章节 + 日期）。
- 只把该子问题最相关的摘要和原句放进当前上下文。

### 4. 在短上下文里回答子问题

```text
仅根据以下证据回答 Q2：<子问题>
逐条列出，每条附证据编号；证据不足写「材料不足」。
证据：
Q2-[1] ...
Q2-[2] ...
```

这个模板是给"只拿得到这些证据的那一小段上下文"用的：在本会话里按它自问自答，或交给子代理都行。交给子代理时，证据原文要一并给它，不要让它回去翻原始材料。

答案写进 `answers/Q<n>.md`，对话里只回结论和证据编号。

### 5. 汇总与冲突校验

汇总只放子答案与证据编号，不重贴原始材料；按下面的骨架写 `汇总报告.md` 并贴给用户：

```text
1. 子问题与状态：Q1..Qn 完成 / 材料不足
2. 结论：每条附证据编号
3. 证据索引：按子问题分组，如 Q1-[1] 路径:行号，或 文档 + 章节 + 日期
4. 冲突与时效：哪两条互斥、哪条更新
5. 材料不足与未决
6. 产物清单：落盘文件路径
```

汇总仍然太长、贴出去会让上下文明显变紧时：先把子答案压成表格、只留结论与证据编号，或分两次汇总（先交 Q1–Q3，确认后再交其余）。

### 6. 会话策略（只提醒，不自动执行）

判据只有一条：这个子问题是否需要另一个子问题的结论。

| 情况 | 策略 | 怎么做 |
| --- | --- | --- |
| 需要前一个结论（有依赖） | 同会话继续 | 直接接着做；上下文偏紧时先写阶段报告落盘，再提醒用户 `/compact`，并说明保留什么、丢弃什么 |
| 不需要其他中间结果（独立） | 提醒换会话 | 输出提醒后停下，由用户自己操作 |
| 判断不了 | 默认同会话 | 开新会话会重新探索，代价更高 |

提醒话术：

```text
检测到该子问题与当前会话其他部分无依赖，建议新开会话处理：
请新建对话（CLI 用 /new，桌面端点「新建对话」New chat），并用 @ 指定相关文件。
新会话起点：读 work/answers/<任务名>/00-子问题清单.md；Q1..Q<n> 已完成；下一步做 Q<n+1>。
```

不要替用户执行新建会话；输出提醒后停在当前会话继续等指令。

## 防腐自检（每写完一个 `answers/Q<n>.md` 就回到本节，不要只在最后做一次）

- 本轮是否读入了与本任务无关的材料？
- 引用的结论里，有没有已被后续材料替代的旧版本？
- 是否出现了未经校验的冲突？
- 产物是否已落盘，索引是否完整？

## 常见错误

- 只做抽象摘要，丢掉数字、日期、否定词，导致结论失真。
- 切得太碎，跨段关系被切断；或整篇读入，等于没切。
- 把所有块摘要又拼回一个大 prompt，等于没拆。
- 摘要只在对话里生成、不落盘，上下文只增不减，拆完比不拆更胀。
- 子问题分别回答后不校验冲突与时效，用上了被推翻的旧结论。
- 把块摘要当最终证据；摘要只适合导航，定论要回到原句。
- 有依赖的子问题硬拆到不同会话，重新探索材料，反而更贵。
- 独立子问题不提醒换会话，旧会话越拖越脏。
